Simple expense calculating app where i practice new React hooks concept.
I also used local storage so progress isn't lost on refresh.

Additional Dependencies downloaded:
1. React-icons for icons (npm install react-icons)
2. UUID for Unique id's (npm install uuid)

To start the App:
npm start